--- a/src/auth/auth.service.implementation.ts
+++ b/src/auth/auth.service.implementation.ts
@@ -33,11 +33,11 @@ export class AuthServiceImplementation implements AuthService {
     try {
       // 创建用户实体
       const user = this.userRepository.create({
-        ...registerUserDto,
-        password: hashedPassword,
-        status: 'active' as UserStatus,
+        email: registerUserDto.email,
+        username: registerUserDto.username,
+        password: hashedPassword,
+        status: UserStatus.ACTIVE,
       });
-
       const savedUser = await this.userRepository.save(user);
 
       // 移除密码字段返回
@@ -57,7 +57,7 @@ export class AuthServiceImplementation implements AuthService {
     try {
       const user = await this.userRepository.findOne({
         where: { email: loginUserDto.email },
-        select: ['id', 'email', 'username', 'password', 'role', 'status'],
+        select: ['id', 'email', 'username', 'password', 'role', 'status', 'fullName'],
       });
 
       if (!user) {
@@ -93,7 +93,8 @@ export class AuthServiceImplementation implements AuthService {
     const payload: JwtPayload = {
       sub: user.id,
       email: user.email,
-      role: user.role,
+      username: user.username,
+      role: user.role as UserRole,
     };
 
     const accessToken = this.jwtService.sign(payload, {
@@ -128,7 +129,8 @@ export class AuthServiceImplementation implements AuthService {
       const newPayload: JwtPayload = {
         sub: payload.sub,
         email: payload.email,
-        role: payload.role,
+        username: payload.username,
+        role: payload.role as UserRole,
       };
 
       const newAccessToken = this.jwtService.sign(newPayload, {
